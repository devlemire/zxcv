import CIP from '../utils/categoriesInPercent';
import determineSelected from '../utils/determineSelected';
import sortCategories from '../utils/sortCategories';

describe("utils/ tests", () => {
  test("True is true.", () => {
    expect( true ).toEqual( true );
  });
});
